export enum EArticleTable {
    article = 'article',
    link = 'link',
    tags = 'tags',
    difficulty = 'difficulty',
    thumb_up = 'thumb_up',
    thumb_down = 'thumb_down',
}

export enum ETableNavigateTypes {
    next = 'next',
    prev = 'prev',
    first = 'first',
    last = 'last',
}
